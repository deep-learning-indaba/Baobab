version: 2.1
jobs:
  build:
    machine: true

    steps:
      - checkout
      - run:
          name: Start container and run tests
          command: |
            docker-compose up -d
            docker-compose run web nosetests -v
